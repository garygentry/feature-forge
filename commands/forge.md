---
description: "View pipeline status and navigate the feature-forge development pipeline"
argument-hint: "<feature-name> (optional — lists all active features if omitted)"
---

Use the Skill tool to invoke `forge` with the provided arguments.
