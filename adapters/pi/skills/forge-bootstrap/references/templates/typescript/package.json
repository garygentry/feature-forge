{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.0",
  "private": true,
  "description": "{{PURPOSE}}",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
